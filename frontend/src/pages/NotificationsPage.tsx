import{useEffect,useState}from"react";import{api}from"../api/client";import type{NotificationItem}from"../types";
export function NotificationsPage(){const[rows,setRows]=useState<NotificationItem[]>([]);const load=()=>api.get('/notifications').then(r=>setRows(r.data));useEffect(()=>{load()},[]);return <div className="page-card"><div className="page-card-head"><div><h2>Notifications</h2><p>Portal, invoice and integration notifications.</p></div><button className="secondary-btn" onClick={async()=>{await api.put('/notifications/read-all');load()}}>Mark all as read</button></div><div className="notification-list">{rows.map(x=><div className={x.isRead?"notification-row":"notification-row unread"} key={x.id}><div><b>{x.title}</b><p>{x.message}</p><small>{new Date(x.createdAt).toLocaleString()}</small></div>{!x.isRead&&<button className="table-btn" onClick={async()=>{await api.put(`/notifications/${x.id}/read`);load()}}>Mark read</button>}</div>)}</div></div>}
