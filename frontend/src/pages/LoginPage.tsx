import { useState } from "react";import {Link,useNavigate} from "react-router-dom";import {login} from "../api/auth";import {getApiErrorMessage} from "../api/client";import {useAuth} from "../auth/AuthContext";
export function LoginPage(){const{setSession}=useAuth();const nav=useNavigate();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);
async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");try{const s=await login(email.trim(),password);setSession(s);nav(s.user.userType==="VENDOR"?"/vendor":s.user.userType==="ADMIN"?"/admin":"/internal",{replace:true})}catch(e){setError(getApiErrorMessage(e))}finally{setLoading(false)}}
return <div className="auth-page"><div className="auth-card"><section className="auth-brand"><img src="/pakistan-cables-logo.png"/><div><h1>Welcome Back!</h1><p>Access purchase orders, GRNs, invoices and payment status securely.</p></div><small>Pakistan Cables Limited</small></section><section className="auth-panel"><form onSubmit={submit}><h2>Sign In</h2><p>Continue to Business Partner's Portal</p><label>Email Address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn" disabled={loading}>{loading?"Signing In...":"Sign In"}</button><div className="auth-divider"><span>New vendor?</span></div><Link className="secondary-btn" to="/signup">Register for Portal Access</Link><small>Vendor must already exist in Oracle EBS before registration.</small></form></section></div></div>}
