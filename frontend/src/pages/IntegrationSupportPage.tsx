import {useEffect,useState}from"react";import{getIntegrationQueue,retryIntegration}from"../api/portal";
export function IntegrationSupportPage(){const[rows,setRows]=useState<any[]>([]);const load=()=>getIntegrationQueue().then(setRows);useEffect(()=>{load()},[]);return <div className="page-card"><div className="page-card-head"><div><h2>Oracle Integration Support</h2><p>Pending, processing, failed and completed invoice interface records.</p></div></div><table className="data-table"><thead><tr><th>Created</th><th>Event</th><th>Invoice</th><th>Status</th><th>Attempts</th><th>Error</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.createdAt}</td><td>{x.eventType}</td><td>{x.aggregateId}</td><td><span className={`status ${x.status==="FAILED"?"red":x.status==="PROCESSED"?"green":"orange"}`}>{x.status}</span></td><td>{x.attemptCount}</td><td>{x.lastError||"-"}</td><td>{x.status==="FAILED"&&<button className="table-btn" onClick={async()=>{await retryIntegration(x.id);load()}}>Retry</button>}</td></tr>)}</tbody></table></div>}
