import{useEffect,useState}from"react";import{api}from"../api/client";import{getAuditLogs}from"../api/portal";
export function UserManagementPage(){const[rows,setRows]=useState<any[]>([]);const[roles,setRoles]=useState<any[]>([]);const[form,setForm]=useState({fullName:"",email:"",userType:"INTERNAL",roleCodes:["SUPPLY_CHAIN"] as string[],password:"",isActive:true,isSuperAdmin:false});const[message,setMessage]=useState("");const load=()=>Promise.all([api.get('/admin/users'),api.get('/admin/roles')]).then(([u,r])=>{setRows(u.data);setRoles(r.data)});useEffect(()=>{load()},[]);async function create(e:React.FormEvent){e.preventDefault();setMessage("");try{await api.post('/admin/users',form);setMessage('User created successfully.');setForm({...form,fullName:'',email:'',password:''});load()}catch(e:any){setMessage(e?.response?.data?.message||e.message)} }return <div className="page-card"><div className="page-card-head"><div><h2>User Management</h2><p className="subtext">Admin / IT controls portal identities, roles and access.</p></div></div><form className="admin-user-form" onSubmit={create}><input placeholder="Full Name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} required/><input type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><select value={form.userType} onChange={e=>setForm({...form,userType:e.target.value})}><option>INTERNAL</option><option>ADMIN</option><option>VENDOR</option></select><select value={form.roleCodes[0]||''} onChange={e=>setForm({...form,roleCodes:[e.target.value]})}>{roles.map(r=><option key={r.code} value={r.code}>{r.name}</option>)}</select><input type="password" placeholder="Temporary Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><button className="primary-btn">Add User</button></form>{message&&<div className={message.includes('success')?'success-note':'form-error'}>{message}</div>}<table className="data-table"><thead><tr><th>Name</th><th>Email</th><th>Type</th><th>Active</th><th>Roles</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.fullName}</td><td>{x.email}</td><td>{x.userType}</td><td><span className={`status ${x.isActive?'green':'red'}`}>{x.isActive?'Active':'Disabled'}</span></td><td>{x.roles?.join(', ')}</td></tr>)}</tbody></table></div>}
export function VendorAccessPage(){const[rows,setRows]=useState<any[]>([]);const load=()=>api.get('/vendor-access').then(r=>setRows(r.data));useEffect(()=>{load()},[]);return <div className="page-card"><div className="page-card-head"><div><h2>Vendor Portal Access</h2><p>Oracle EBS remains the vendor master. This page manages portal access only.</p></div></div><div className="success-note">New vendors can self-register after Oracle supplier details and registered email are verified by OTP.</div><table className="data-table"><thead><tr><th>Vendor</th><th>Oracle Vendor ID</th><th>User</th><th>Email</th><th>Access</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={`${x.vendorId}-${x.userId}`}><td>{x.vendorName}</td><td>{x.oracleVendorId}</td><td>{x.fullName}</td><td>{x.email}</td><td><span className={`status ${x.accessActive&&x.isActive?'green':'red'}`}>{x.accessActive&&x.isActive?'Enabled':'Disabled'}</span></td><td><button className="table-btn" onClick={async()=>{await api.put(`/vendor-access/${x.userId}/active`,{isActive:!(x.accessActive&&x.isActive)});load()}}>{x.accessActive&&x.isActive?'Disable':'Enable'}</button></td></tr>)}</tbody></table></div>}
export function AuditPage(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{getAuditLogs().then(setRows).catch(()=>setRows([]))},[]);return <div className="page-card"><h2>Audit Logs</h2><table className="data-table"><thead><tr><th>Time</th><th>User</th><th>Action</th><th>Entity</th><th>Correlation</th></tr></thead><tbody>{rows.map((x:any)=><tr key={x.id}><td>{new Date(x.createdAt).toLocaleString()}</td><td>{x.userId||'-'}</td><td>{x.action}</td><td>{x.entityType}</td><td>{x.correlationId||'-'}</td></tr>)}</tbody></table></div>}
export function RolesPermissionsPage(){const[roles,setRoles]=useState<any[]>([]);const[permissions,setPermissions]=useState<any[]>([]);const[selectedRole,setSelectedRole]=useState<string>('');const[selected,setSelected]=useState<string[]>([]);const[message,setMessage]=useState('');useEffect(()=>{Promise.all([api.get('/admin/roles'),api.get('/admin/permissions')]).then(([r,p])=>{setRoles(r.data);setPermissions(p.data);if(r.data.length)setSelectedRole(r.data[0].id)})},[]);useEffect(()=>{if(!selectedRole)return;api.get(`/admin/roles/${selectedRole}/permissions`).then(r=>setSelected(r.data))},[selectedRole]);const role=roles.find(r=>r.id===selectedRole);async function save(){setMessage('');try{await api.put(`/admin/roles/${selectedRole}/permissions`,{permissionIds:selected});setMessage('Permissions saved successfully.')}catch(e:any){setMessage(e?.response?.data?.message||e.message)}}return <div className="page-card"><div className="page-card-head"><div><h2>Roles & Permissions</h2><p>Policy-based access control for portal roles.</p></div></div><div className="role-permission-layout"><div><label>Role</label><select value={selectedRole} onChange={e=>setSelectedRole(e.target.value)}>{roles.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></div><div className="permission-grid">{permissions.map(p=><label key={p.id} className="permission-check"><input type="checkbox" disabled={role?.code==='ADMIN'} checked={role?.code==='ADMIN'||selected.includes(p.id)} onChange={e=>setSelected(e.target.checked?[...selected,p.id]:selected.filter(x=>x!==p.id))}/><span><strong>{p.code}</strong><small>{p.name}</small></span></label>)}</div></div>{role?.code!=='ADMIN'&&<button className="primary-btn" onClick={save}>Save Permissions</button>}{message&&<div className={message.includes('success')?'success-note':'form-error'}>{message}</div>}</div>}
export function SystemConfigurationPage(){const[data,setData]=useState<any>(null);useEffect(()=>{api.get('/admin/system').then(r=>setData(r.data))},[]);return <div className="page-card"><div className="page-card-head"><div><h2>System Configuration</h2><p>Safe runtime configuration status. Secrets are never exposed here.</p></div></div>{data&&<div className="system-config-grid"><div><small>Environment</small><strong>{data.environment}</strong></div><div><small>Oracle Read Connection</small><strong>{data.oracleConfigured?'Configured':'Missing'}</strong></div><div><small>Oracle Invoice Posting</small><strong>{data.oracleInvoicePostConfigured?'Configured':'Pending contract'}</strong></div><div><small>OTP Email</small><strong>{data.smtpEnabled?'SMTP enabled':'Development mode'}</strong></div><div><small>Max Upload</small><strong>{data.maxUploadSizeMb} MB</strong></div><div><small>Retention</small><strong>{data.dataRetentionDays==='0'?'Awaiting PCL policy':`${data.dataRetentionDays} days`}</strong></div></div>}<div className="success-note">Production values are controlled through the backend environment configuration so credentials and secrets are not editable in the browser.</div></div>}
