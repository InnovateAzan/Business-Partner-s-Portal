import {useEffect,useState} from "react";import {getMySupplier}from"../api/portal";import type{OracleSupplier}from"../types";
export function VendorProfilePage(){const[s,setS]=useState<OracleSupplier|null>(null);useEffect(()=>{getMySupplier().then(setS)},[]);return <div className="page-card profile-page"><h2>Vendor Profile</h2><p className="subtext">Vendor master data is read from Oracle EBS. Portal users cannot amend the supplier master here.</p><dl><dt>Vendor Name</dt><dd>{s?.vendorName||"-"}</dd><dt>Vendor ID</dt><dd>{s?.vendorId||"-"}</dd><dt>Supplier Number</dt><dd>{s?.supplierNumber||"-"}</dd><dt>Site Code</dt><dd>{s?.vendorSiteCode||"-"}</dd><dt>Operating Unit</dt><dd>{s?.operatingUnit||"-"}</dd><dt>Email</dt><dd>{s?.email||"-"}</dd><dt>Phone</dt><dd>{s?.phone||"-"}</dd></dl></div>}
